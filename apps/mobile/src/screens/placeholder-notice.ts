/**
 * The synthetic M0 placeholder notice shared by every mobile screen.
 * Kept as a pure constant so the text contract asserted by the Maestro
 * smoke journey (`maestro/flows/smoke.yaml`) is unit-testable.
 */
export const PLACEHOLDER_NOTICE = "Coming in M6+" as const;
