{
  "name": "@orbb/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Pure domain contracts for ORBB: canonical ids, evidence labels, provenance primitives, aggregate types, and state-machine invariants. Zero runtime dependencies.",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "eslint": "^9.30.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.30.0",
    "vitest": "^3.2.4"
  }
}
