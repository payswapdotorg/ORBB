{
  "name": "@orbb/consent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Consent boundary for ORBB: will own grants, revocation, and deny-by-default access evaluation. M0 boundary package — re-exports the access-plane types it will own.",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "@orbb/domain": "workspace:*",
    "eslint": "^9.30.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.30.0",
    "vitest": "^3.2.4"
  }
}
