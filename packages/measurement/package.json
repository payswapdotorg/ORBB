{
  "name": "@orbb/measurement",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Measurement boundary for ORBB: will own metrics, methods, plans, schedules, and observation capture/validation. M0 boundary package — re-exports the measurement-plane types it will own.",
  "main": "./dist/index.js",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@eslint/js": "^9.30.0",
    "@orbb/domain": "workspace:*",
    "eslint": "^9.30.0",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.30.0",
    "vitest": "^3.2.4"
  }
}
